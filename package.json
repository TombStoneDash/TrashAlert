{
  "name": "trashalert-factory",
  "version": "0.0.0",
  "private": true,
  "description": "Factory tooling for TrashAlert: city audits, sampling, import scripts. Not the runtime — the FastAPI app under app/ and the static frontend under frontend/ are the product.",
  "scripts": {
    "audit:cities": "tsx scripts/audit-cities.ts"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.103.3",
    "tsx": "^4.21.0"
  },
  "engines": {
    "node": ">=20"
  }
}
